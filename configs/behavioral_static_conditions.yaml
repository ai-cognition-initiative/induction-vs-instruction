# Example task configuration for run.py
#
# Usage:
#   uv run python run.py configs/example.yaml \
#     --model openrouter/google/gemini-2.0-flash-001

protocol: behavioral          # "behavioral" or "prediction"

conditions:
  - neutral
  - value_pattern
  - value_target
  - factual_pattern 
  - factual_target 
  - token_pattern_states 
  - token_pattern_countries

n_turns:                       # list of N values (induction pressure)
  - 1
  - 3
  - 5
  - 10
  - 15
  - 20
  - 25

instruction_templates:
  - instruction_hint
  - instruction_no_hint
n_trials: 10
